SUPABASE_DB_CONNECTION_STRING=
MIGRATION_DB_CONNECTION_STRING=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# URL base del frontend. Se usa para construir el parámetro `redirect_to`
# que GoTrue (Supabase Auth) incluye en los correos (invitación y
# recuperación de contraseña). En desarrollo es http://localhost:5174
# (puerto fijado por vite.config.ts → server.port).
RACPD_FRONTEND_URL_BASE=http://localhost:5174
